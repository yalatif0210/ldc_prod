package com.markov.lab.repository;

import com.markov.lab.entity.IntrantType;
import org.springframework.data.jpa.repository.JpaRepository;

public interface IntrantTypeRepository extends JpaRepository<IntrantType, Long> {
}
