package com.markov.lab.repository;

import com.markov.lab.entity.SynthesisType;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SynthesisTypeRepository extends JpaRepository<SynthesisType, Long> {
}
